# 발표 정리

- 본 프로젝트는 SaaS 고객사의 churn을 account 단위로 예측하는 프로젝트이다.
- 핵심은 이탈 이후 정보가 아니라 이탈 전 신호를 feature로 설계한 점이다.
- `churn_events`는 data leakage를 막기 위해 예측용 train table에서 제외하고, analysis/XAI 단계에서만 활용하였다.
- 결측치는 단순 삭제 대신 값 대체와 missing flag를 함께 사용하여 ML, DL, XAI 확장성을 확보하였다.
